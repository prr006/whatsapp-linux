/**
 * M14 TEST STUB (not upstream code) — minimal stand-in for 50.1
 * js/misc/signalTracker.js (registerDestroyableType is a no-op here; the
 * harness tracks lifetimes explicitly).
 */

export function registerDestroyableType(_klass) {}
