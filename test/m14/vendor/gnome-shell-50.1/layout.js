/**
 * M14 TEST STUB (not upstream code) — minimal stand-in for 50.1
 * js/ui/layout.js (messageTray.js constructs one MonitorConstraint).
 */

export class MonitorConstraint {
  constructor(params) {
    this.params = params;
  }
}
